{
  "name": "@facio/products",
  "version": "0.0.0",
  "description": "Single source of truth for Abbeygate product manifests + validation profiles. Each product (motor/home/travel) owns one declarative manifest (rendering authority) and one validation profile (data authority). Both FE and BE consume from this package — there are no per-side mirrors.",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@facio/validation": "*",
    "libphonenumber-js": "*",
    "zod": "*"
  }
}
