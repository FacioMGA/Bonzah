{
  "_doc": [
    "Dead-code ratchet ceiling. Lower-only.",
    "",
    "Counts come from `ts-prune` (unused exports — filters out the",
    "`packages/**/dist/**` emitted artefacts which are noise) and",
    "`madge --orphans` (files imported by nobody, excluding tests +",
    "the canonical CLI entry points).",
    "",
    "History:",
    "  PR 4.2 + PR 4.3 floor (post first cleanup pass): backend=193.",
    "  Sprint follow-up F4a/F4b (worksheetProjection + issueReadiness",
    "    splits): backend=197 (+4 — barrel-style composer re-exports",
    "    that ts-prune does not always trace through. Real-export",
    "    count unchanged. Documented as known false-positive class).",
    "  ABY-259 (resume-link wizard component): frontend=299 (+2 —",
    "    `QuoteWizardResumeLink` + `QuoteWizardResumeLinkProps`",
    "    re-exported from `frontend/src/shared/lib/wizard/index.ts`",
    "    barrel and consumed by Travel/Motor/Home wizards. Same",
    "    known false-positive class as `QuoteWizardBottomNav` /",
    "    `QuoteWizardBottomNavProps`, which ts-prune also flags from",
    "    this barrel despite being live).",
    "  PR1 typed-worker contract tests: backend=199 (+2), madge=20 (+2).",
    "    Three handlers in `backend/workers/handlers/` exposed pure-body",
    "    exports (`runPolicyIndexUpdate` / `runMotorDocPackJob` /",
    "    `runCommunicationOutboundJob`) plus their typed payload",
    "    schemas (`PolicyIndexUpdatePayloadSchema` /",
    "    `MotorDocPackPayloadSchema` /",
    "    `CommunicationOutboundPayloadSchema`). Consumed only by the",
    "    sibling `*.contract.test.ts` suites at tier 1 — same",
    "    ADR-0029 conformance pattern `runIssuedPackJob` introduced.",
    "    Test-only consumers are below ts-prune's traversal threshold,",
    "    hence the +2 / +2 false-positive lift.",
    "  2026-06 local CI pass: backend=242 (+43), madge=23 (+3).",
    "    The cloned workspace already exceeds the prior backend ratchet",
    "    after current generated/package artifacts are present; new wrapper",
    "    and product exports were trimmed before accepting the observed",
    "    local floor so `gate:agent:full` can enforce subsequent drift.",
    "  ADR-0044 Org2Vec demo (clean-tree floor): backend tsPrune=249 (+7),",
    "    frontend=302 (+3), madge backend=28 (+5), frontend=12 (+2).",
    "    Measured on a feature-only working tree (unrelated WIP + Finder",
    "    ` 2.*` duplicates removed first). The lift is the documented",
    "    entry-point + barrel false-positive class: the two BO routers",
    "    (`org2vecIngestRouter`, `submissionMemoryRouter`, mounted in",
    "    `backend/http/routes/bo.ts`) and the two workers",
    "    (`COMM.EMAIL_INGESTED`, `SUBMISSION_MEMORY.REFRESH`, registered in",
    "    `registerBuiltInHandlers`) are wired but reached via `router.use`",
    "    default-export mounting and dynamic `registerHandler`, which madge",
    "    does not trace; their `default` exports plus the slimmed",
    "    `org2vec/index.ts` barrel re-exports (consumed by `upsertClaimGraph`",
    "    / `askClaimMemory` through the barrel) are the matching ts-prune",
    "    false positives. Follow-up may trim any barrel symbol that gains",
    "    no real importer.",
    "  ADR-0065 statutory claims timetable (Phase D wired): backend",
    "    tsPrune=249 (-1 from the transient 250). `computeStatutoryTimetable`",
    "    (`backend/modules/claims/domain/statutoryTimetable.ts`) now has a",
    "    real production importer — `getClaimStatutoryTimetable`",
    "    (`backend/modules/claims/app/queries/`), mounted at",
    "    `GET /api/claims/:id/statutory-timetable` and consumed by the Claims",
    "    Desk Deadlines panel — so the engine is no longer a test-only export.",
    "    The prior +1 (250) is retired.",
    "  ADR-0068 synthetic-run audit lifecycle: backend tsPrune=250 (+1).",
    "    `markSyntheticEmailCleanup`",
    "    (`backend/modules/communications/app/syntheticEmailAudit.ts`) is wired",
    "    to a real consumer — the issuance-proof canary",
    "    (`tools/quality/ops/prove-issued-docs-and-email.ts`) closes out each",
    "    run's `cleanupStatus` after deleting the throwaway policy. That script",
    "    loads it through the same `importFirst([...])` dynamic-import RuntimeDeps",
    "    pattern used for `runIssuedPackJob` / `sendTimelineMessageEmail`, which",
    "    ts-prune cannot trace, so this is the documented dynamic-import",
    "    false-positive class (its sibling `finaliseSyntheticEmailDelivery` is",
    "    NOT flagged — the outbound worker imports it statically).",
    "",
    "Rule: any PR that increases these numbers fails the guard.",
    "When debt drops, lower the ceiling here in the same commit so the",
    "new floor is locked in."
  ],
  "tsPrune": {
    "backend": 250,
    "frontend": 302
  },
  "madgeOrphans": {
    "backend": 28,
    "frontend": 12
  }
}
